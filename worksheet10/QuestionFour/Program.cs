using System;

namespace QuestionFour
{
    class Program
    {
        static void Main(string[] args)
        {

            //Avarage: Some of the overloads return empty or null if the sequence is empty or contains only null values
            //FirstOrDefault/LastOrDefault: Returns empty or  null  if no match is found
            //SingleOrDefault: Returns Empty or Null if no match is found
        }
    }
}
